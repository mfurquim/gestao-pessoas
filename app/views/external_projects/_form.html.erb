<%= form_for(@external_project) do |f| %>
  <% if @external_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@external_project.errors.count, "error") %> prohibited this external_project from being saved:</h2>

      <ul>
      <% @external_project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
