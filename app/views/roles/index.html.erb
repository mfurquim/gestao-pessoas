<ul class="nav nav-tabs">
<% first = true %>
<% @roles_users.each do |role,users| %>
    <li <%= 'class="active"' if first %>><a data-toggle="tab" href="#<%= role %>"><%= translate("role."+role) %></a></li>
    <% first = false %>
<% end %>
</ul>
<% first = true %>
<div class="tab-content">
<% @roles_users.each do |role,users| %>
    <div id="<%= role %>" class="tab-pane fade <%= 'in active' if first %>">
    <% first = false %>
        <table class="table table-hover">
              <thead>
                <tr>
                  <th>Email</th>
                  <th>Ver Perfil</th>
                  <th>Permissões</th>
<!--                  <th>Perfil</th> -->
                </tr>
              </thead>

              <tbody>
                <% users.each do |user| %>
                  <tr>
                    <td><%= user.email %></td>

                    <td><%= link_to 'Perfil', [user,user.personal_information] %></td>
                    <td><%= link_to 'Modificar', user_edit_role_path(user) %></td>
<!--                    <td><%= link_to 'Modificar', edit_user_registration_path(user) %></td>-->
                  </tr>
                <% end %>
              </tbody>
            </table>
    </div>
<% end %>
</div>
<% if @roles_users.empty? %>
  <h1> No users </h1>
<% end %>
