<%= form_for(@extra_skill) do |f| %>
  <% if @extra_skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@extra_skill.errors.count, "error") %> prohibited this extra_skill from being saved:</h2>

      <ul>
      <% @extra_skill.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
